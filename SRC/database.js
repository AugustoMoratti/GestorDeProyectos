//Aqui debemos hacer la coneccion a la base de datos,

/*
const mysql = require('promise-mysql');

const connection = mysql.createConnection({
  host: 'localhost',
  user: 'root',
  password: '',
  database: 'nombreBaseDeDatos'
});

function getConnection() {
  return connection;
}

module.exports = { getConnection };
*/